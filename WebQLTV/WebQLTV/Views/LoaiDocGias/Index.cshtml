@model IEnumerable<WebQLTV.Models.LoaiDocGia>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenLoaiDocGia)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenLoaiDocGia)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDLoaiDocGia }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDLoaiDocGia }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDLoaiDocGia })
        </td>
    </tr>
}

</table>
