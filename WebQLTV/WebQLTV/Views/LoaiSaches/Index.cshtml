@model IEnumerable<WebQLTV.Models.LoaiSach>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenLoaiSach)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenLoaiSach)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDLoaiSach }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDLoaiSach }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDLoaiSach })
        </td>
    </tr>
}

</table>
