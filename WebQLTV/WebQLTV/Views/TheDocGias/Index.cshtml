@model IEnumerable<WebQLTV.Models.TheDocGia>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LoaiDocGia.TenLoaiDocGia)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TenTK)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LoaiDocGia.TenLoaiDocGia)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TenTK)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDTheDocGia }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDTheDocGia }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDTheDocGia })
        </td>
    </tr>
}

</table>
