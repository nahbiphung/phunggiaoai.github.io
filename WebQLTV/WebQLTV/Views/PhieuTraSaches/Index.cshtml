@model IEnumerable<WebQLTV.Models.PhieuTraSach>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IdChiTietPhieuTra)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IdChiTietPhieuTra)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDPhieuTra }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDPhieuTra }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDPhieuTra })
        </td>
    </tr>
}

</table>
